services:
  - type: web
    name: trainup-api
    runtime: php
    buildCommand: "composer install --no-dev"
    startCommand: "php -S 0.0.0.0:$PORT -t ."
    envVars:
      - key: PHP_VERSION
        value: 8.2
